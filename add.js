//Let's Start

// Function to add two numbers
function addNumbers(a, b) {
    return a + b;
  }
  
  // Calling the function and storing the result
  let result = addNumbers(12, 13);
  
  // Displaying the result in the console
  console.log("The sum is: " + result);
  